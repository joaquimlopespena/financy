{
  "name": "financy",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "start": "npm run start --prefix backend",
    "studio": "npm run studio --prefix backend"
  },
  "dependencies": {
    "zustand": "^5.0.12"
  }
}
